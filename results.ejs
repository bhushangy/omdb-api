
<% include partials/header.ejs %>

<h1>Results</h1>
<% if(data["Search"]!=undefined) { %>


 
<% data["Search"].forEach(function(movie){ %> <!--see here data is a single object and inside data is a list of objects that u are iterating..movie here is the each abd every object...so accessing ele inside object thru movie[""]-->
<ul>
<li>
     <strong>
         <%=movie["Title"]%> 
    </strong>- <%=  movie["Year"]  %>
</li>
</ul> 

<%})%> 
 
<a href = "/" type="button" >Search again</a>

<% } else { %>    <!-- note here the previous if closing { shud be paired with th else part's opening...like this ->.... } else {...-->
   <p>no movies found</p>
    <a href = "/" type="button" >Search again</a> 
<% } %>

    <% include partials/footer.ejs %>

